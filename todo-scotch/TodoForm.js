import React from 'react';
import { StyleSheet, Text, View, TextInput } from 'react-native';

export default class TodoForm extends React.Component {
  render() {
    return (
      <TextInput value="type here"></TextInput>
    );
  }
}

// const styles = StyleSheet.create({});
